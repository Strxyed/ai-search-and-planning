(define (domain lunar-extended)
    (:requirements :strips :typing)

    (:types

    )

    (:predicates

    )

    (:action action_name
        :parameters ()
        :precondition (and)
        :effect (and)
    )

)